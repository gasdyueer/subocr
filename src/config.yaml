pipeline:
  maas:
    enabled: false
  
  ocr_api:
    api_host: localhost
    api_port: 1234
    api_path: /api/generate  # Use LM Studio native endpoint
    model: glm-ocr   # Required: specify model name
    api_mode: lmstudio
  
  enable_layout: false  # Recommended for initial testing